import fs from 'node:fs/promises';
import path from 'node:path';
import assert from 'node:assert/strict';
const out=path.resolve('output/playwright/madagin-world-progress/ocean-release-20260906');
const before=JSON.parse(await fs.readFile(path.join(out,'before-final.json')));
const after=JSON.parse(await fs.readFile(path.join(out,'after-final.json')));
const parity=after.cases.flatMap(a=>a.views.map(v=>{
 const b=before.cases.find(c=>c.viewport.width===a.viewport.width).views.find(c=>c.id===v.id);
 const position=Math.hypot(...v.camera.position.map((x,i)=>x-b.camera.position[i]));
 const look=Math.hypot(...v.camera.look.map((x,i)=>x-b.camera.look[i]));
 assert.equal(position,0);assert.ok(look<.001);assert.ok(v.clockHeld&&b.clockHeld);
 return {width:a.viewport.width,view:v.id,position,look,oceanSeconds:40};
}));
await fs.writeFile(path.join(out,'camera-parity.json'),JSON.stringify(parity,null,2)+'\n');
let full;try{full=JSON.parse(await fs.readFile(path.join(out,'motion/review.json')))}catch{}
const data={before,after,full};
await fs.writeFile(path.join(out,'review.html'),`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Madagin — Ocean motion review</title>
<style>*{box-sizing:border-box}body{margin:0;background:#0b1817;color:#f0f4ee;font:16px/1.55 system-ui}main{max-width:1440px;margin:auto;padding:32px}h1{font-size:clamp(32px,4vw,54px);line-height:1.1}p{max-width:88ch;color:#c7d2cc}a{color:#a9c6ff}select,button{padding:10px;font:inherit;color:white;background:#213a34;border:1px solid #739484}label{display:inline-block;margin:12px 24px 12px 0}#compare{position:relative;aspect-ratio:1440/900;background:#000;max-height:900px}#compare img{position:absolute;width:100%;height:100%;object-fit:contain}#after{clip-path:inset(0 0 0 50%)}input{width:100%}.tags{display:flex;justify-content:space-between}video{width:100%;max-height:820px;background:#000}.note{border-left:3px solid #b9d09b;padding-left:18px}table{border-collapse:collapse}td,th{padding:10px;text-align:left;border-bottom:1px solid #476156}</style>
<main><small>MADAGIN / SEPTEMBER 6, 2026 / ACTUAL WEBSITE CAPTURES</small><h1>Shoreward surf, living water.</h1><p>A bounded motion revision: six dispersive swells, continuously filtered wind ripples, and advancing foam fronts with fading wakes. Existing land, coast, vegetation, atmosphere and camera are retained. This remains an authored ocean; no fluid solver, overturning crest, spray or real scene reflection is claimed.</p><p><a href="https://projectwebbing.vercel.app/">Live website</a> · <a href="camera-parity.json">Camera and simulation parity</a> · <a href="accepted-rejected.json">Decisions and remaining defects</a></p>
<label>View <select id="view"><option value="ridge-coast">Ridge / About</option><option value="terminal-coast">Terminal / About</option></select></label><label>Size <select id="size"><option value="1440">1440 × 900</option><option value="390">390 × 844</option></select></label>
<div class="tags"><span>Before · a9de2ee</span><span>Selected ocean revision</span></div><div id="compare"><img id="before" alt="Previous accepted ocean"><img id="after" alt="Revised ocean at the same camera and ocean time"></div><label style="width:100%">Reveal comparison <input id="split" type="range" min="0" max="100" value="50"></label><p class="note">Same resolution, camera, lighting and ocean clock (40 seconds). Other environmental animation phases differ. Recordings release that clock before each 12-second held observation; the freeze/release and checkpoint jumps are diagnostic setup, not part of the natural journey.</p>
<h2>Watch motion</h2><label>Recording <select id="phase"><option value="after">Selected — held ocean</option><option value="before">Before — held ocean</option><option value="full">Selected — uninterrupted full journey</option></select></label><video id="video" controls preload="metadata"></video><p><button id="play">Play / pause</button> <button data-seek="20">First held ocean</button> <button data-seek="40">Final held ocean</button></p><p>Held recordings contain the first coast view followed by a checkpoint jump and the final coast view. Full journey includes loading, the complete automatic flight, then held Ocean and Blog. This is browser emulation on the Windows host, not physical-device qualification.</p>
<h2>Reference and limits</h2><p>The <a href="https://dlnr.hawaii.gov/dsp/wp-content/blogs.dir/34/files/2014/09/h_napali.jpg">DLNR Nāpali photo</a> and the opening aerial sequence of <a href="https://www.youtube.com/watch?v=kxJIVCGPac8">DLNR's Kalalau video</a> show far richer broken surf, cliff structure and continuous varied canopy. The photograph and actual YouTube playback were inspected this run. The newer Vimeo reference required human verification; no challenge was bypassed.</p><p>Broad water and world realism remain 2/5. Regular wave structure, simplified foam and reflections remain visible. Independent human still/motion equivalence and physical devices remain unverified. Domain and mailbox are unverified; inquiries still copy/download locally.</p>
<h2>Preserved, rejected geographic trials</h2><p>The complete NOAA catchment, revised reflectance/canopy, and finer source-normal trial remain local. The large landforms are useful, but smooth cliff detail, artificial distant canopy and an overly fast route fall below the accepted world. The next proof is a small native-spacing cliff tile with separate rock detail and grounded three-dimensional vegetation.</p><div id="budgets"></div></main>
<script>const data=${JSON.stringify(data).replaceAll('<','\\u003c')};const el=id=>document.getElementById(id);function video(){const p=el('phase').value,w=Number(el('size').value),r=data[p];if(!r)return;const c=r.cases.find(c=>c.viewport.width===w);el('video').src=(p==='full'?'motion/':'')+c.recording;}function refresh(){const s=el('size').value,v=el('view').value;el('compare').style.aspectRatio=s==='390'?'390/844':'1440/900';for(const p of ['before','after'])el(p).src=p+'-final/'+s+'/'+v+'.png';video();}el('play').onclick=()=>{const v=el('video');v.paused?v.play():v.pause();};document.querySelectorAll('[data-seek]').forEach(b=>b.onclick=()=>{el('video').currentTime=Number(b.dataset.seek);el('video').play();el('video').scrollIntoView({block:'start'});});el('view').onchange=refresh;el('size').onchange=refresh;el('phase').onchange=video;el('split').oninput=()=>el('after').style.clipPath='inset(0 0 0 '+el('split').value+'%)';refresh();if(data.full)el('budgets').innerHTML='<h2>Host budgets</h2><table><tr><th>Width</th><th>World / limit MiB</th><th>Ready / 8 s</th><th>Heap / limit MiB</th><th>Held frames</th></tr>'+data.full.cases.map(c=>'<tr><td>'+c.viewport.width+'</td><td>'+(c.worldBytes/1048576).toFixed(2)+' / '+(c.viewport.width<700?8:24)+'</td><td>'+(c.meaningfulWorldMs/1000).toFixed(2)+'</td><td>'+(c.heapBytes/1048576).toFixed(1)+' / '+(c.viewport.width<700?192:384)+'</td><td>'+c.budgetChecks.steadyFrames+'</td></tr>').join('')+'</table>';</script></html>`);
console.log(JSON.stringify({cameraParity:parity,fullMotion:!!full}));
