import fs from 'node:fs/promises';
import path from 'node:path';
const out=path.resolve('output/playwright/madagin-world-progress/terrain-readiness-release-20260908');
const read=async p=>JSON.parse(await fs.readFile(path.join(out,p),'utf8'));
const before=await read('../cliff-profile-release-20260908/motion-public/review.json');
const local=await read('motion/review.json');
let production=null;try{production=await read('motion-public/review.json')}catch{}
const data={before,local,production};
await fs.writeFile(path.join(out,'review.html'),`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Madagin — Cliff and startup review</title>
<style>*{box-sizing:border-box}body{margin:0;background:#0c1817;color:#f4f4ef;font:16px/1.6 system-ui}main{max-width:1440px;margin:auto;padding:32px}h1{font-size:clamp(32px,4vw,52px);line-height:1.15}p{max-width:95ch;color:#d0d9d1}a{color:#aac9ff}select,button,input{font:inherit;background:#203b33;color:white;border:1px solid #668575;padding:9px}label{display:inline-block;margin:10px 20px 10px 0}.pair{display:flex;gap:24px;flex-wrap:wrap}.pair figure{margin:0;max-width:390px;flex:1;min-width:250px}.pair img{width:100%}video{width:100%;max-height:calc(100vh - 40px);background:#000}td,th{padding:10px;border-bottom:1px solid #456354;text-align:left}table{border-collapse:collapse}</style>
<main><p>MADAGIN / SEPTEMBER 8, 2026</p><h1>A stronger cliff, with a complete opening.</h1><p>The native cliff profile published in c8371f5 is retained. A follow-up fixes the loading race found in its compact production recording: foliage could reveal an empty scene before terrain arrived. The existing still now remains visible until terrain and foliage are committed and rendered. <a href="https://projectwebbing.vercel.app/">Open the live website</a> · <a href="../cliff-profile-release-20260908/review.html">Cliff geometry: 22 before/after views and original evidence</a>.</p>
<h2>Delay terrain, allow foliage to finish</h2><p>Same compact viewport and held terrain request. Before: the previous production release prematurely declares readiness and shows floating trees. After: the selected local correction retains the existing still and waits. This is loading behavior; the still is not evidence of rendered realism.</p><div class="pair"><figure><figcaption>Before · c8371f5 · premature readiness</figcaption><img src="ordering-baseline-reproduction/390-terrain-held.png" alt="Trees visible with terrain withheld on previous production"></figure><figure><figcaption>After · waits for terrain</figcaption><img src="ordering-local/390-terrain-held.png" alt="Existing fallback still visible while terrain is withheld"></figure><figure><figcaption>After · terrain and foliage ready</figcaption><img src="ordering-local/390-terrain-ready.png" alt="Actual rendered cliff after terrain arrives"></figure></div>
<h2>Original uninterrupted flights</h2><label>Size <select id="size"><option value="1440">1440 × 900 desktop</option><option value="390">390 × 844 compact</option></select></label><label>Recording <select id="record"><option value="production">Selected public flight</option><option value="local">Selected local flight</option><option value="before">Previous public flight with startup defect</option></select></label><p><button id="play">Play recording</button> <button id="pause">Pause recording</button> <label>Playback seconds <input id="seek" type="number" min="0" value="0"></label><button id="seek-go">Seek recording</button></p><video id="motion" controls preload="metadata"></video><p id="video-note"></p><p>Each original recording contains loading, the full automatic journey, and 12-second held Ocean and Blog observations. The previous compact recording shows missing terrain around 3 seconds. Local and public captures are kept separately. No loading placeholder or moving camera establishes environmental realism.</p>
<h2>Measured budgets</h2><div id="budgets"></div><p>Headless Chrome on this Windows host; viewport emulation, not physical-phone/GPU/thermal evidence. Targets: world transfer 24/8 MiB desktop/compact; meaningful readiness 8 seconds; heap 384/192 MiB; held rAF p95/p99 33.3/50 ms. The corrected readiness marker requires terrain plus foliage.</p><h2>Acceptance and limits</h2><p>The useful cliff profile remains accepted. Its authored coastal collar, sparse layered vegetation, unconnected local drainage, synthetic inland water and atmosphere remain unresolved. Whole-world realism stays 2/5 diagnostic; Milestone B is open. Independent human still/motion equivalence and domain/mailbox delivery remain unverified. The inquiry continues to copy/download a brief. The linked cliff review includes the real DLNR photograph and footage references, provenance and matched-camera limits.</p></main>
<script>const data=${JSON.stringify(data).replaceAll('<','\\u003c')};const el=id=>document.getElementById(id);function refresh(){const mode=el('record').value,report=mode==='before'?data.before:mode==='production'?(data.production??data.local):data.local,c=report.cases.find(c=>c.viewport.width===Number(el('size').value));const prefix=mode==='before'?'../cliff-profile-release-20260908/motion-public/':mode==='production'&&data.production?'motion-public/':'motion/';el('motion').src=prefix+c.recording;el('video-note').textContent=mode==='production'&&!data.production?'Public verification pending; selected local recording shown.':report.origin+' — original uninterrupted recording.';}el('size').onchange=refresh;el('record').onchange=refresh;el('play').onclick=()=>{el('motion').play();el('motion').scrollIntoView({block:'start'});};el('pause').onclick=()=>el('motion').pause();el('seek-go').onclick=()=>{el('motion').currentTime=Number(el('seek').value);};const measured=data.production??data.local;el('budgets').innerHTML='<p>'+measured.origin+'</p><table><tr><th>Width</th><th>World transfer</th><th>Readiness</th><th>Heap</th><th>Held frames</th></tr>'+measured.cases.map(c=>'<tr><td>'+c.viewport.width+'</td><td>'+(c.worldBytes/1048576).toFixed(2)+' MiB · '+(c.budgetChecks.worldBytes?'PASS':'FAIL')+'</td><td>'+(c.meaningfulWorldMs/1000).toFixed(2)+' s · '+(c.budgetChecks.meaningfulWorld?'PASS':'FAIL')+'</td><td>'+(c.heapBytes/1048576).toFixed(0)+' MiB · '+(c.budgetChecks.heap===true?'PASS':'UNVERIFIED/FAIL')+'</td><td>'+Math.max(...c.views.map(v=>v.samples.p95)).toFixed(1)+' ms p95 · '+(c.budgetChecks.steadyFrames?'PASS':'FAIL')+'</td></tr>').join('')+'</table>';refresh();</script></html>`);
console.log(path.join(out,'review.html'));
